/**
 * tensor-flow 页面
 */

const md = `
TensorFlow.js 是一个能够让开发者在前端直接运行机器学习模型的 JavaScript 库。
它允许你无需将数据发送到服务器，便可以在浏览器中运行模型进行推理，这不仅减少了延迟，还可以更好地保护用户的隐私数据。
通过 TensorFlow.js，前端开发者能够轻松实现图像分类、物体检测和姿态估计等功能。

## 创建一个简单的 TensorFlow.js 模型
首先，你需要创建一个 TensorFlow.js 模型。
你可以使用 tf.sequential() 方法来创建一个序列模型，或者使用 tf.loadLayersModel() 方法来加载一个已保存的模型。

## 加载数据集
然后，你需要加载数据集。
你可以使用 tf.data.Dataset() 方法来创建一个数据集，或者使用 tf.load() 方法来加载一个已保存的数据集。


1. @tensorflow/tfjs
核心定位：基础框架库 (Core Library)
处理内容：它不直接处理具体的业务（如识别猫或狗），而是提供在 JavaScript 环境中进行机器学习所需的底层能力。
主要功能：
定义和张量操作（Tensors）。
构建、训练和部署机器学习模型。
提供 WebGL、WebAssembly 等后端加速。
加载其他模型（如加载下面提到的 MobileNet 等）。
类比：就像汽车的引擎和底盘，没有它，其他模型无法在 JS 中运行。

2. @tensorflow-models/mobilenet
核心定位：图像分类 (Image Classification)
处理内容：识别整张图片的主要内容是什么。
主要功能：
输入一张图片。
输出图片中最可能的一个或多个类别（例如：“猫”、“咖啡杯”、“汽车”）。
通常不包含物体的具体位置坐标。
特点：模型轻量，速度快，适合移动端和网页端。
典型场景：相册自动分类、以图搜图的基础识别。

3. @tensorflow-models/coco-ssd
核心定位：物体检测 (Object Detection)
处理内容：识别图片中有哪些物体以及它们在哪里。
主要功能：
输入一张图片。
输出多个物体的类别 + 边界框坐标 (Bounding Box)。
基于 COCO 数据集训练，能识别 80 种常见物体（人、车、狗、瓶子等）。
特点：可以检测同一张图片中的多个不同物体。
典型场景：安防监控（检测入侵者）、自动驾驶（检测行人车辆）、自动计数。

4. @tensorflow-models/posenet
核心定位：人体姿态估计 (Pose Estimation)
处理内容：识别人体的关键骨骼点。
主要功能：
输入一张包含人的图片或视频流。
输出人体的关键点坐标（如：鼻子、眼睛、肩膀、手肘、手腕、膝盖、脚踝等）。
可以连接这些点形成“骨架”。
特点：专注于人体结构，不识别物体类别，只识别人体动作。
典型场景：健身动作纠正、体感游戏、舞蹈教学、虚拟试衣。

包名称              类型         核心任务          输入              输出结果
@tensorflow/tfjs   核心库       机器学习基础设施    数据/模型定义      张量/计算结果 (无特定语义)
mobilenet          预训练模型    图像分类           图片             "这是一只猫" (类别 + 概率)
coco-ssd           预训练模型    物体检测           图片             "这里有一只猫 (坐标)，那里有一个人 (坐标)"
posenet            预训练模型    姿态估计           图片/视频         "鼻子在 (x,y)，左手在 (x,y)..." (骨骼点)

`